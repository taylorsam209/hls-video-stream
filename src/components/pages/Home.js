import React, {Component} from 'react';
import {HomeWrapper} from '../Theme';

export default class Home extends Component {

    render() {
        return(
           <HomeWrapper>
               <h2>Welcome to Camera App</h2>
            </HomeWrapper> 
        )
        
    }
}